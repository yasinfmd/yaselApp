import styled from 'styled-components';
import { size, color, flexbox, space, borderRadius, width, border, height, borderLeft } from 'styled-system';
import { View } from 'react-native';
const Box = styled(View)(size, color, flexbox, space, borderRadius, width, border, height);

export default Box;
