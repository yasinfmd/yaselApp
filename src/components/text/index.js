import styled from 'styled-components';
import { typography, color, letterSpacing, space } from 'styled-system';
import { Text } from 'react-native';
const Typography = styled(Text)(typography, color, letterSpacing, space);

export default Typography;
