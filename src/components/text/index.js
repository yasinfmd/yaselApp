import styled from 'styled-components';
import { typography, color, letterSpacing } from 'styled-system';
import { Text } from 'react-native';
const Typography = styled(Text)(typography, color, letterSpacing);

export default Typography;
