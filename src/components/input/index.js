import styled from 'styled-components';
import { size, color, space, borderRadius, width, border, height, borderColor, margin, typography, marginBottom } from 'styled-system';
import { TextInput } from 'react-native';
const Input = styled(TextInput)(size, color, space, borderRadius, width, border, height, borderColor, margin, typography);

export default Input;
